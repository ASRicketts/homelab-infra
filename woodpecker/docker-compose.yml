# Woodpecker CI — CI/CD server + agent.
# Server listens for Gitea webhooks, dispatches jobs to the agent.
# Agent runs each pipeline step in a Docker container.

services:
  woodpecker-server:
    image: woodpeckerci/woodpecker-server:v3
    container_name: woodpecker-server
    restart: unless-stopped
    environment:
      # Gitea integration
      - WOODPECKER_OPEN=true
      - WOODPECKER_HOST=http://woodpecker.lab
      - WOODPECKER_GITEA=true
      - WOODPECKER_GITEA_URL=http://gitea.lab # how Woodpecker reaches Gitea
      - WOODPECKER_GITEA_CLIENT=${WOODPECKER_GITEA_CLIENT}
      - WOODPECKER_GITEA_SECRET=${WOODPECKER_GITEA_SECRET}

      # Agent server commumicationnication secret (must match agent's)
      - WOODPECKER_AGENT_SECRET=${WOODPECKER_AGENT_SECRET}

      # Admin users (gets admin rights in Woodpecker UI)
      - WOODPECKER_ADMIN=asricketts # My Gitea username

    extra_hosts:
      # Map gitea.lab to the VM's host gateway, which Traefik is listening on.
      # Lets the Woodpecker container resolve gitea.lab the same way a browser
      # on the LAN does. Without this, Docker's internal DNS has no idea what
      # gitea.lab is and the OAuth token exchange call fails.
      - "gitea.lab:host-gateway"

    volumes:
      # Persistent storage for Woodpecker's SQLite db (pipeline history, etc.)
      - ./server-data:/var/lib/woodpecker

    networks:
      - traefik

    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.woodpecker.rule=Host(`woodpecker.lab`)"
      - "traefik.http.routers.woodpecker.entrypoints=web"
      - "traefik.http.services.woodpecker.loadbalancer.server.port=8000"

  woodpecker-agent:
    image: woodpeckerci/woodpecker-agent:v3
    container_name: woodpecker-agent
    restart: unless-stopped
    depends_on:
      - woodpecker-server
    command: agent
    environment:
      # Talk to the server over gRPC on its internal port
      - WOODPECKER_SERVER=woodpecker-server:9000
      - WOODPECKER_AGENT_SECRET=${WOODPECKER_AGENT_SECRET}
      - WOODPECKER_BACKEND_DOCKER_NETWORK=traefik # the network agent-launched containers will join

      # How many concurrent pipeline steps this agent can run
      - WOODPECKER_MAX_WORKFLOWS=2

    extra_hosts:
      - "gitea.lab:host-gateway" # If the pipelines need to talk to Gitea (e.g. to clone repos), this lets
      # agent-launched containers resolve gitea.lab the same way a browser on the
      # LAN does. Without this, Docker's internal DNS has no idea what gitea.lab is and git clone

    volumes:
      # The agent needs the Docker socket — it spins up containers per step
      - /var/run/docker.sock:/var/run/docker.sock

    networks:
      - traefik

networks:
  traefik:
    external: true
